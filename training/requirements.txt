# training/requirements.txt
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Dependencies installed by SageMaker into the PyTorch 2.2 / py311 container
# before sagemaker_train_spatial.py runs.
#
# Keep minimal — SageMaker already provides:
#   torch, torchvision, numpy, boto3, sagemaker, s3fs
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Model weights & training
scikit-learn>=1.3.0           # roc_auc_score for val eval
Pillow>=10.0.0                # Image loading + SBI augmentation
numpy>=1.24.0                 # SBI alpha mask math
timm>=0.9.12                  # EfficientNet-B4 as fallback backbone

# WandB (optional — won't crash if API key not set)
wandb>=0.16.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
